<Window x:Class="Buttons.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Buttons"
             mc:Ignorable="d" 
             Height="200" Width="300"
        Title="Идентификатор элемента">
    <StackPanel>
        <Label Content="Узнать ID элемента"
               Margin = "5"/>
        <Button Content="Количество труб" Command="{Binding SelectCommandPipes}"
                Margin = "5"/>
        <Button Content="Объем всех стен" Command="{Binding SelectCommandWalls}"
                Margin = "5"/>
        <Button Content="Количество дверей" Command="{Binding SelectCommandDoors}"
                Margin = "5"/>
    </StackPanel>
</Window>
